# This file is kept for reference but the workspace is now defined at the repo root.
# See /Cargo.toml for workspace configuration.
#
# To build vendor packages, run from repo root:
#   cargo build -p vendor
#   cargo build -p keeper
#   cargo build -p scripts
